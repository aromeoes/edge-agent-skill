# Optional — only needed for write operations and attendee search
SOLA_AUTH_TOKEN=           # Social Layer JWT for creating/updating events
EDGEOS_BEARER_TOKEN=       # EdgeOS JWT for attendee directory search
